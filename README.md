# Social-Media-API
A backend social media application built using FastAPI that provides secure user authentication and post management through RESTful APIs. The project allows users to register, log in using JWT-based authentication, and perform CRUD operations on posts while enforcing proper authorization and security practices.
